export type PerformanceMetrics = { nodeCount: number; focusedNodeCount: number; visibleNodeCount: number; renderedNodeCount: number; renderedEdgeCount: number; collapsedNodeCount: number; lastLayoutMs: number; lastSearchMs: number; lastSaveMs: number; lastExportMs: number; slowOperations: Array<{ name: string; durationMs: number }> };
export const EMPTY_PERFORMANCE_METRICS: PerformanceMetrics = { nodeCount: 0, focusedNodeCount: 0, visibleNodeCount: 0, renderedNodeCount: 0, renderedEdgeCount: 0, collapsedNodeCount: 0, lastLayoutMs: 0, lastSearchMs: 0, lastSaveMs: 0, lastExportMs: 0, slowOperations: [] };
export function recordMetric(metrics: PerformanceMetrics, name: string, durationMs: number): PerformanceMetrics { const slowOperations = durationMs > 100 ? [...metrics.slowOperations, { name, durationMs }].slice(-10) : metrics.slowOperations; return { ...metrics, slowOperations }; }
