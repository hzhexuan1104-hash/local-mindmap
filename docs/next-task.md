# v1.5 后续任务建议

## v1.5 第一批已完成

- 已完成顶部菜单收纳、左侧资源区重构、右侧检查器 Tabs 和画布控制条轻量化。
- 已保留 `.lmind` 保存打开、全部导入导出、节点类型包、模板包、备注、查找替换、插件和画布交互入口。
- 已通过 `npm run build` 与 `npm run test`；本批未新增业务功能、npm 依赖或文件格式字段。

## v1.5 第二批视觉精修已完成

- 已完成全局视觉变量、暖白画布、悬浮轻面板、模板卡片、检查器分组和中心主题层级精修。
- 已统一菜单、右键菜单、表单、Tabs、控制条、弹窗、选择和拖拽反馈。
- 已完成 1280×720 本地视觉回归，无页面横向溢出和控制台错误。
- 已通过 `npm run build` 与 `npm run test`，未修改 package 文件和 `.lmind` 结构。

## v1.5 后续建议

1. 在 Windows Tauri 窗口和 2K / 4K 高 DPI 环境进行视觉回归。
2. 补充浏览器端 UI 自动化，覆盖菜单、面板、Tabs、右键菜单与文件选择入口。
3. 使用多层、多主题和长备注样例检查节点密度、连线和编辑器滚动细节。
4. 继续完善统一图标资源，但不为此引入重量级组件库。
5. 准备 v1.5.0 发布候选版和跨平台手动验收。

后续继续保持纯本地、无登录、无同步、无协同，不修改 `.lmind` 基础结构。

# 历史任务记录：v1.3.0 发布后建议

## 优先建议

1. Tauri 环境补齐与桌面端验证：
   - 安装 Rust / Cargo。
   - 在 Windows 上运行 `npm run tauri:dev` 和 `npm run tauri:build`。
   - 验证桌面窗口启动、加载当前 Vite 应用、保存/打开 `.lmind`、导入导出和图片导出。
   - 继续验证 macOS、Linux、统信 UOS、银河麒麟和国产 CPU 架构。
2. 节点拖拽体验补强：
   - 验证 1000 节点导图拖拽响应。
   - 优化拖拽过程中的性能和选中态。
   - 评估是否需要吸附、辅助线或简单避让。
   - 评估兄弟节点拖拽排序、批量层级调整和更智能的排版辅助。
3. 多选能力继续增强：
   - 增加批量移动、批量样式编辑。
   - 优化框选后的批量操作体验。
   - 评估多选复制 / 剪切 / 粘贴后的批量移动和批量样式编辑体验。
4. 自动化测试继续补充：
   - 增加 UI 组件测试或端到端测试。
   - 覆盖保存、打开、导入导出、插件启停、模板、主题、拖拽、多选、右键菜单等主流程。
5. UI 细节打磨：
   - 检查小屏和高 DPI 表现。
   - 优化顶部工具区密度。
   - 优化弹窗、右键菜单、备注面板和长文本溢出。

## 后续功能

- 兄弟节点拖拽排序和批量层级调整。
- 框选后的批量移动和批量样式编辑。
- 粘贴位置细节增强，例如吸附、辅助线或更智能的避让。
- 更完整的主题和线条样式配置。
- 插件签名、插件目录和沙箱执行能力。
- 桌面端真实安装包 Release。

## v1.3 当前批次：节点类型共享基础版

本批已聚焦节点类型包导入 / 导出，不继续展开其他 v1.3 功能。

- 已完成：导出节点类型包 JSON。
- 已完成：导入节点类型包 JSON。
- 已完成：导入后写入 localStorage 本地节点类型库。
- 已完成：ID 冲突处理和导入结果提示。
- 已完成：节点类型包纯函数测试覆盖。
- 待后续：更完整的端到端 UI 自动化、跨浏览器手动验收、桌面端打包环境验证。

后续仍不做云同步、用户登录、远程节点类型市场、权限系统或第三方 JavaScript 执行。

## v1.3 第二批：模板包导入 / 导出基础版

本批聚焦模板包导入 / 导出，不展开其他 v1.3 功能。

- 已完成：导出模板包 JSON。
- 已完成：导入模板包 JSON。
- 已完成：导入后写入 localStorage 本地模板库。
- 已完成：模板 ID 冲突处理和导入结果提示。
- 已完成：模板包纯函数测试覆盖。
- 待后续：更完整的端到端 UI 自动化、跨浏览器手动验收、桌面端打包环境验证。

后续仍不做云同步、用户登录、远程模板市场、权限系统或第三方 JavaScript 执行。

## v1.3 第三批：共享包体验收口

本批聚焦节点类型包和模板包体验收口，不展开云同步、登录、远程市场、权限系统或其他新功能。

- 已完成：统一节点类型包 / 模板包导入结果提示。
- 已完成：优化非法 JSON、kind 不匹配、空包和重复导入提示。
- 已完成：补充共享包边界测试，覆盖非法 kind、空包、重复导入和冲突重命名。
- 已完成：新增 `docs/share-packs.md` 示例共享包说明文档。
- 已完成：更新验收清单、发布清单、项目状态、README 和 v1.3 release notes。
- 已完成：正式 v1.3.0 发版版本号升级。
- 待后续：更完整的浏览器端到端自动化、跨浏览器手动验收和桌面端打包环境验证。

v1.3.0 发布准备阶段，`package.json` 和 `package-lock.json` 已同步为 `1.3.0`。

## v1.3.0 发布前交互修复：框选与画布平移

本次只修复画布框选和平移的交互冲突，不新增复杂框选模式、不恢复兄弟节点拖拽排序、不修改 `.lmind` 结构。

- 已完成：画布空白区域普通左键拖拽用于平移画布。
- 已完成：Shift + 画布空白区域左键拖拽用于框选节点，结果替换当前选择。
- 已完成：框选命中改为节点中心点在框选矩形内才选中，减少边缘重叠导致的误选。
- 已完成：框选、节点拖拽和平移过程中阻止浏览器原生文字选区，输入框和备注编辑区仍可选择文本。
- 已完成：补充 `boxSelection.test.ts` 覆盖中心点命中、zoom / pan 坐标换算、Shift 框选和普通空白拖拽平移入口。
- 已完成：修复 root / 中心主题向左或向上拖动被布局边界卡住的问题，允许负坐标 position 保存和打开恢复。
- 已完成：修复非 Shift 框选后旧主选节点残留高亮的问题，`selectedNodeId` 与 `selectedNodeIds` 保持一致。
- 已完成：root 仍不能被拖成其他节点子节点，但 root position 自由拖动不受结构保护影响。
- 已完成：点击画布空白区域清空选择，不再默认选中中心主题，后续 Ctrl / Shift 多选不会自动带上 root。
- 已完成：框选视觉矩形和节点命中统一使用同一套 screen/world 坐标转换，修复 zoom / pan 后视觉与命中偏差。

## v1.3.0 发布后建议

- 执行 GitHub Pages 发布流程并确认线上预览可打开。
- 在 Chrome / Edge 中手动回归节点类型包、模板包、`.lmind` 保存打开和 v1.2 编辑能力。
- 后续继续补充端到端自动化，不在 v1.3.0 发布前新增功能。
# v1.4 后续任务建议

当前第一批已完成桌面 Native 插件目录和 manifest 管理底座。下一步建议：

1. 在本机执行 `rustup default stable` 或配置公司内网认可的 Rust toolchain 后，回归 `npm run tauri:dev` 和 `npm run tauri:build`。
2. 在 Windows 上手动验证桌面插件目录创建、manifest 安装、扫描、启用、禁用、卸载。
3. 在 macOS / Linux 上验证 Tauri app data dir 映射到预期插件目录。
4. 继续保持 v1.4 边界：不加载 DLL、不执行第三方代码、不做远程插件市场。
5. v1.5 再评估 Windows DLL 加载实验版，包括 ABI、可信来源、签名、崩溃隔离和权限模型。

## v1.4 第二批后续任务建议

当前第二批目标是桌面端本地配置与内网分发准备，不做大规模状态迁移。

1. 验证 `get_desktop_config_dir` 和 `ensure_desktop_config_dir` 在 Windows / macOS / Linux 上均映射到预期 app data dir。
2. 继续保持 Web / GitHub Pages 的 `localStorage` 行为，不因桌面配置目录不可用而崩溃。
3. 后续按优先级迁移：桌面插件 registry、自定义节点类型、自定义模板、最近打开文件、主题偏好。
4. 在迁移每类配置前补充兼容策略和回归测试，避免破坏 Web JSON 插件、节点类型包、模板包和 `.lmind` 保存打开。
5. 完成 `npm run tauri:build` 环境验证后，记录 Windows 安装包、绿色包和 IT 软件中心分发路径。
6. v1.4 继续只管理 manifest 和本地配置目录，不加载 DLL、不执行 DLL、不执行第三方代码。

## v1.4.0 发布后建议

1. 在 GitHub 上创建或更新从 `v1.4-dev` 到 `main` 的 PR。
2. 确认 CI、GitHub Pages 构建和必要手动验收通过。
3. 合并到 `main` 后创建 `v1.4.0` tag。
4. 基于 `docs/release-notes-v1.4.0-draft.md` 创建 GitHub Release。
5. 将 Windows MSI / NSIS 安装包作为 Release 附件或交给公司内网分发流程。
6. v1.5 开始前先完成 DLL 插件 ABI、安全审查、可信来源、签名、崩溃隔离和回滚方案设计。
