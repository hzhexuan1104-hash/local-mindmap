# 下一步任务建议

## 优先建议

1. 对插件系统做手动验收：
   - 启用 / 禁用内置主题包。
   - 启用 / 禁用内置图标包。
   - 启用 / 禁用 TXT 导出插件。
   - 安装合法和非法本地插件 JSON。
   - 刷新页面验证插件状态保留。
2. 补充真实示例插件 JSON 文件，方便验收和演示。
3. 做一次完整回归测试，覆盖保存、打开、导入导出、备注、模板、节点类型、主题和撤销重做。

## 后续功能

- 节点复制 / 剪切 / 粘贴。
- 拖拽节点调整层级与顺序。
- 框选多个节点和批量操作。
- 更完整的主题和线条样式配置。
- 官方模板库。
- 桌面端打包。
- 插件签名、插件目录和沙箱执行能力。

