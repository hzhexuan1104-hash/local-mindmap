# 会议纪要 Workflow 插件

本示例使用 `workflow.actions` 在选中节点下创建会议背景、参会人员、关键议题、
讨论结论和行动项。它只执行宿主解析、校验过的声明式 actions，不执行代码。

插件声明导图写权限，因此新安装仍为 `trusted=false`，首次执行写入动作时继续走
宿主权限确认。一次工作流作为一个历史批次，可使用 Ctrl+Z / Ctrl+Y 撤销和重做。
