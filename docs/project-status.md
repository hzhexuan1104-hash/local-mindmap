# 项目状态

更新时间：2026-06-24

## 已完成

- React + TypeScript + Vite 基础工程。
- 基础节点新增、删除、编辑。
- 子节点、同级节点。
- 节点文本自动换行和基础自动布局。
- `.lmind` 保存和打开。
- Markdown 备注、实时预览、独立放大预览。
- Markdown / Excel / JSON 导入导出。
- Excel 导入列映射界面已实现，支持有表头和无表头数据。
- Markdown 导入导出通过 `LMIND_NODE` 区分节点标题和备注标题。
- 全局查找替换。
- 模板管理增强：分类、备注、搜索、排序、筛选、缩略图。
- 官方模板库基础版已实现：
  - 内置 8 个官方默认模板。
  - 官方模板不可删除。
  - 官方模板支持搜索、分类筛选和排序。
  - 官方模板可直接用于新建思维导图。
- 自定义节点类型增强：图标、形状、字体样式。
- 图片导出 PNG/JPG。
- 撤销 / 重做，历史记录不少于 50 步。
- 画布缩放、平移、一键居中。
- 节点折叠 / 展开。
- 主题系统，内置 5 套主题。
- 插件管理系统基础版已实现：
  - 本地 JSON 插件安装。
  - 插件启用、禁用、卸载。
  - 3 个内置示例插件。
  - 插件主题、插件图标、TXT 导出能力。
- 桌面端打包准备：
  - 新增桌面端打包方案文档。
  - 保持 Web 版本和 GitHub Pages 构建方式不变。
  - 明确 Tauri / Electron 后续评估路线。
- 信创适配说明：
  - 覆盖统信 UOS、银河麒麟、Windows 10+、macOS 11+。
  - 覆盖 x86/x64、ARM64、飞腾、鲲鹏、龙芯等架构风险。
- 自动化测试基础：
  - 新增 Vitest。
  - 新增 `npm run test`。
  - 覆盖 Markdown 导入导出、JSON 校验、Excel 核心解析、TXT 导出和插件 manifest 校验。
- 性能压测工具已实现：
  - 支持生成 100 / 500 / 1000 节点测试导图。
  - 显示节点总数、最大层级、生成耗时和内容生成耗时。
  - 支持复制性能测试结果为 Markdown。
- Release 检查文档已完成：
  - 新增 `docs/release-checklist.md`。
  - 覆盖构建、测试、GitHub Pages、回归、导入导出、插件和性能检查。
- README 和验收测试文档。
- GitHub Pages 部署。

## 本次构建结果

- `npm run build` 通过。
- `npm run test` 通过。
- Vite 提示打包后主 chunk 超过 500 kB，这是体积提醒，不是构建错误。

## 安全状态

- 当前插件系统不执行第三方 JS。
- 不使用 `eval` 或 `new Function`。
- 不加载远程脚本。
- 插件数据保存在 localStorage。
- 用户文件仍通过本地浏览器文件能力导入导出。
- 桌面端准备阶段未新增联网能力或云端服务。

## 待确认

- 插件主题或插件节点类型在跨设备共享 `.lmind` 时，是否需要将插件 manifest 一并嵌入文件。
- 后续是否需要桌面端插件目录、插件签名或沙箱执行能力。
- 桌面端真实打包尚未完成，需要后续在目标系统和目标架构上验证。
- 自动化测试目前以纯函数单元测试为主，尚未覆盖浏览器端交互和端到端流程。
